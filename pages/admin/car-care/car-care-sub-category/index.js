import Breadcrumb from '@/components/common/Breadcrumb'
import Layout from '@/components/common/Layout'
import Head from 'next/head'
import React, { useEffect, useState } from 'react'
import axios from 'axios'
import { convertTimeStamp, dateFormat } from '@/components/Helper'
import Link from 'next/link'
import useSWR from 'swr';
import { useRouter } from 'next/router'
import Paginationn from '@/components/common/Pagination'

export default function Index() {

    const router = useRouter()
    const [carCare, setCarCare] = useState([])
    const { page } = router.query;
    const carCareList = async () => {
        await axios.get(`${process.env.NEXT_PUBLIC_URL}admin/car-care-sub-category-list`, {
            headers: {
                'token': typeof window !== "undefined" ? localStorage.getItem("lr-admin-token") : false,
            }
        }).then(function (res) {
            if (res.data.status == 1) {
                setCarCare(res.data.data)
            }
        }).catch(function (error) {
            if (error.response.data.status === 0) {
                alert(err.response.data.message);
            }
        });
    }

    const updateStatus = async (status, id) => {

        await axios.get(`${process.env.NEXT_PUBLIC_URL}admin/active-deactive-car-care-sub-category?id=${id}&status=${status}`, {
            headers: {
                'token': typeof window !== "undefined" ? localStorage.getItem("lr-admin-token") : false,
            }
        }).then(function (res) {
            if (res.data.status == 1) {
                console.log(res.data.data)
                setService(res.data.data)
                alert(res.data.message);
            }
        }).catch(function (error) {
            if (error.response.data.status === 0) {
                alert(err.response.data.message);
            }
        });
    }
    useEffect(() => {
        carCareList()
    }, [])

    return (
        <>
            <Head>
                <title>LR | Car Care Sub Category</title>
                <meta name="description" content="Generated by create next app" />
                <meta name="viewport" content="width=device-width, initial-scale=1" />
                <link
                    href="/img/lr-favicon.ico"
                    rel="icon"
                    media="(prefers-color-scheme: light)"
                />
                <link
                    href="/img/lr-favicon-dark.ico"
                    rel="icon"
                    media="(prefers-color-scheme: dark)"
                />
            </Head>
            <Layout>
                <Breadcrumb title="Car Care Sub Category" addBtn="Add Car Care Sub Category" url="/admin/car-care/car-care-sub-category/add-car-care-sub-category" />
                <div className="main-data">

                    <div className="filters">

                        <div className="wrapper">

                            <div className="search common-div">

                                <input type="text" placeholder="Search here" />
                                <button></button>

                            </div>

                            <div className="custom-select common-div">

                                <select>

                                    <option>Brands</option>
                                    <option>Brands</option>

                                </select>

                            </div>

                            <div className="custom-select common-div">

                                <select>

                                    <option>Type</option>
                                    <option>Brands</option>

                                </select>

                            </div>

                            <div className="custom-select common-div">

                                <select>

                                    <option>Status</option>
                                    <option>Brands</option>

                                </select>

                            </div>

                            <div className="custom-select common-div">

                                <input className="calender-style" type="text" placeholder="dd/mm/yyyy" />

                            </div>

                            <div className="custom-select common-div">

                                <input className="calender-style" type="text" placeholder="dd/mm/yyyy" />

                            </div>

                            <div className="custom-select button-style common-div">

                                <button>Apply Filters</button>

                            </div>

                            <div className="custom-select button-style common-div">

                                <button className="border-style">Clear Filters</button>

                            </div>

                        </div>

                    </div>

                    <table>
                        <thead>
                            <tr>
                                <th style={{ width: '100px', textAlign: 'center' }}>ID</th>
                                <th>Car Care Category</th>
                                <th>Sub Category</th>
                                <th>Status</th>
                                <th>Created At</th>
                                <th>Update At</th>
                                <th style={{ width: '200px' }}>Action</th>
                            </tr>
                        </thead>
                        <tbody>

                            {carCare != '' && carCare.length > 0 && carCare?.map((services, i) => {
                                const count = 0
                                return (
                                    <tr key={i}>
                                        <td>
                                            {i + 1}
                                        </td>
                                        <td>
                                            {services.service_category && services.service_category.car_care_category_name}
                                        </td>
                                        <td>
                                            {services.car_care_sub_category_name && services.car_care_sub_category_name}
                                        </td>

                                        <td>
                                            <button className={`active small-btn ${services.status === 1 ? 'active' : 'inactive'} `} >{services.status === 1 ? 'active' : 'in-active'} </button>
                                            {/* <button className={`active small-btn ${services.status === 1 ? 'active' : 'inactive'} `} onClick={() => updateStatus(services.status === 1 ? 2 : 1, services._id)}>{services.status === 1 ? 'active' : 'in-active'} </button> */}
                                        </td>
                                        <td>
                                            {convertTimeStamp(services.createdAt)}
                                        </td>
                                        <td>
                                            {convertTimeStamp(services.updatedAt)}
                                        </td>
                                        <td>
                                            <div className="table-buttons">
                                                <Link href={`/admin/car-care/car-care-sub-category/add-car-care-sub-category?id=${services._id}`}>  <button className='duplicate'>Edit</button>
                                                </Link>
                                            </div>
                                        </td>
                                    </tr>
                                )
                            })
                                // :
                                // <tr>
                                //     <td>Data not avaiable</td>
                                // </tr>
                            }
                        </tbody>
                    </table>
                    {/* <div>
                        <Paginationn page={page} const rowCount={service !== undefined ? service : ''} />
                      
                    </div> */}
                </div>
            </Layout>
        </>
    )
} 